const express = require('express')
const router = express.Router()
//const { saveSquadPlayers } = require('../controllers/apiControllers');

//router.route('/').post(saveSquadPlayers);

module.exports = router;